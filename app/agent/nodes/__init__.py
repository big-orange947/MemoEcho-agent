# =============================================================================
# agent.nodes - LangGraph 图节点
# -----------------------------------------------------------------------------
# 每个节点 = 一个纯函数: 接收 AgentState,返回要合并进 State 的 dict。
# 图本身(graph.py)只负责"节点顺序 + 条件路由",业务逻辑全部在节点里。
#
# 阅读顺序(与事件流一致):
#   ingest → retrieve → reason → act(循环) → reflect → finalize
# =============================================================================
