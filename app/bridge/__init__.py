# =============================================================================
# bridge 包 - 外部渠道接入
# -----------------------------------------------------------------------------
# 目前只有 QQ(NapCat)。每个渠道 = 一个 Bridge 类,提供:
#   - 发送消息
#   - 查询信息(登录态/好友列表)
#   - 事件上报接收(webhook)
# 上层(agent/main)不直接依赖具体渠道,通过注入的 sender 回调调用。
# =============================================================================
