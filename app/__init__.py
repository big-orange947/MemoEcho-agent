# =============================================================================
# app 包 - Memo Echo v2 后端
# -----------------------------------------------------------------------------
# 目录速览(从事件流的角度理解依赖方向):
#
#   bridge/     QQ 接入层(NapCat),把 QQ 消息变成内部事件
#       ↓
#   events.py   事件模型 + 事件总线(统一入口)
#       ↓
#   agent/      LangGraph 对话代理(核心大脑)
#       ├─ state.py    状态定义(LangGraph 的 StateSchema)
#       ├─ graph.py    图的组装(节点+边+checkpoint)
#       ├─ prompts.py  所有提示词集中管理
#       └─ nodes/      每个图节点一个文件,便于单步理解
#       ↓
#   tools/      LangChain 工具(消息发送/联系人/记忆)
#   services/   业务服务(会话/目标/配置的增删改查)
#   api/        桌面端 HTTP + SSE(实时推送)
#   db.py       SQLite 连接与建表
#   config.py   配置加载(环境变量 + YAML)
#   main.py     FastAPI 入口 + CLI 启动命令
# =============================================================================
